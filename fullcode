<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gift Card System</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f4f4f9;
        }

        #gift-card {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        button {
            margin-top: 10px;
            padding: 10px 20px;
            background-color: #6200ee;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #3700b3;
        }
    </style>
</head>
<body>
    <div id="gift-card">
        <h1>Gift Card</h1>
        <p>Card Number: <span id="card-number">1234 5678 9012 3456</span></p>
        <p>Expiration Date: <span id="exp-date">12/25</span></p>
        <p>Security Code: <span id="security-code">123</span></p>
        <p>Balance: $<span id="card-balance">100</span></p>
        <button id="edit-balance-btn">Edit Balance</button>
    </div>

    <script>
        document.getElementById('edit-balance-btn').addEventListener('click', function() {
            let newBalance = prompt("Enter new balance:");
            if (newBalance !== null && !isNaN(newBalance)) {
                // Update card balance for display
                document.getElementById('card-balance').textContent = parseFloat(newBalance).toFixed(2);
                // POS compatibility function
                updatePOSBalance(parseFloat(newBalance));
            } else {
                alert("Please enter a valid number.");
            }
        });

        // Function to update balance in POS system
        function updatePOSBalance(balance) {
            // Example API call to update balance in the POS system
            fetch('/api/update-balance', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ balance: balance })
            })
            .then(response => response.json())
            .then(data => {
                console.log('Balance updated in POS:', data);
            })
            .catch(error => {
                console.error('Error updating balance in POS:', error);
            });
        }
    </script>
</body>
</html>
